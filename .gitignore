## Custom
nohup.out
_*
# Dotfiles are ignored wholesale, so anything meant to be tracked is
# re-admitted below. .github in particular is silently lost without this.
.*
!.github/
!.editorconfig
!.gitignore
.tools
*-lock.*
node_modules
creds
build
release
output
/assets/models/
/captures/
/tmp/
dfgb-*.log
/reports/
NEXT*
DV-*

## Build and check artifacts
coverage.xml
htmlcov/
.coverage*
