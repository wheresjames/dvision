[pytest]
# apps/ is a source root, not a package: the applications under it import each
# other as `dsim.dsim` and `dcmn.window`, with no `apps.` prefix. Listing it
# here is what the entry points do for themselves in their own bootstrap.
pythonpath = . apps
markers =
    nightly: opt-in longer-running calibration and endurance coverage
